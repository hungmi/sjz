<h1 class="page-header">
  <%= t "admin.items.index.header" %>
  <%= link_to admin_items_path, class: "btn btn-link btn-xs-sm" do %>
  	<i class="fa fa-reply"></i>
  	<%= t "back" %>
  <% end %>
</h1>
<h3 class="mb-2"><%= @employee.name %></h3>
<%= render partial: "admin/items/item", collection: @employee_items, as: :item %>